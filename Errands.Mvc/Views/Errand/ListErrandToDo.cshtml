
@model IEnumerable<GetErrandToDoViewModel>
@{
    ViewData["Title"] = "I took";
}
<div class="main__table">
    <table class="table">
        <caption class="table__caption">My list</caption>
        <tr>
            <th class="table__title">Title</th>
            <th class="table__title">Price</th>
            <th class="table__title">Description</th>
            <th class="table__title">Done</th>
            <th class="table__title"></th>
        </tr>
        @foreach (var user in Model)
        {
            <tr>
                <td class="text-right">@user.Title</td>
                <td class="table__price">@user.Cost</td>
                <td class="text-right">@user.Description</td>
                <td class="@(user.Done?"txt-success":"txt-danger")">@user.Done</td>
                @*<td><a asp-controller="Message" asp-action="Chat" asp-route-userId="@user.NeedlyUserId">@user.NeedlyUserId</a></td>*@
            <td>
                @if (user.NeedlyUserId != null)
                {
                    <form asp-action="Chat" asp-controller="Message" method="post">
                        <input type="hidden" name="receiverUserId" value="@user.NeedlyUserId" />
                        <button type="submit">Go to chat</button>
                    </form>
                }
                @*@if (user.NeedlyUserId != null)
                {
                    
                    <a asp-controller="Message" asp-action="Chat" asp-route-userId="@user.NeedlyUserId">Go to chat</a>
                }*@
            </td>
            </tr>
        }
    </table>
    
</div>